# DriftScore プロジェクト要件定義

このドキュメントは、DriftScoreの企画・設計段階での議論をまとめたものです。
Claude Codeはこのファイルをプロジェクトのコンテキストとして読み込んでください。

---

## プロジェクト概要

- **アプリ名**: DriftScore
- **掲載先**: 個人開発ブログ「DriftCraft」（https://drift-craft.com/）の制作実績の1つとして掲載予定
- **ブランド表記ルール**:
  - 表示・ロゴ・UI: `DriftScore`（キャメルケース。DriftCraftとの表記統一）
  - ドメイン: `drift-score.com` 想定（DriftCraft本体が `drift-craft.com` のハイフンありのため統一）
  - コード上の識別子・AWSリソース名: `driftscore`（すべて小文字、ハイフン/アンダースコア区切り。例: `driftscore-api`, `driftscore-lambda-arrange`）
- **運用方針**: 個人が特定されないブランド名義で展開

### 既存ブランド（DriftCraft）の技術構成（参考・踏襲予定）
- インフラ: AWS CDK（TypeScript）/ Lambda / DynamoDB / API Gateway / CloudFront
- テーマカラー: `#0a422f`（緑系）、ダークモード対応

---

## コアコンセプト

ユーザーが入力した既存のメロディーをベースに、指定したジャンル（ジャズ、サンバ、クラシックなど）に合わせた伴奏を自動生成し、楽譜として出力・編集・再生・印刷できるWeb作曲支援アプリ。

## 機能要件

### 1. メロディー入力・伴奏自動生成
- 既存メロディーを入力（初期はMIDIアップロード or ピアノロールUIを想定）
- ジャンル指定による伴奏自動生成（ジャズ、サンバ、クラシックなど）

### 2. アレンジ制御
- 忠実なアレンジ ⇔ 崩し（フェイク・変形）の度合いを調整可能
- 曲のブレイク（休止・タメ）の挿入度合いを調整可能

### 3. 楽器編成
- **吹奏楽・フルオーケストラなど大編成のプリセット**に対応
- **クラリネット1本、トランペット1本、ギター、エレキベースのみ、のような自由なカスタム編成**にも対応
- 編成プリセット＋カスタム編成の二層構造を想定
- 楽器数に応じた「和声・リズムの役割再配分」ロジックが必要（少人数編成では制約下での最適配置）
- 移調楽器（B♭管クラリネット・トランペット、F管ホルンなど）の自動移調、音域バリデーションが必要

### 4. 楽譜・スコア出力
- 自動生成された伴奏を含め、フルスコア・各楽器のパート譜を出力・表示
- **楽譜の調整機能**（優先度順）:
  1. 表示調整（段組み、余白、五線サイズ、ページサイズ）
  2. 演奏記号調整（強弱記号、アーティキュレーション、スラー）
  3. 部分編集＋部分再生成（1音単位のピッチ/リズム変更、特定小節だけ再生成）
  - ※フル譜面エディタ（Finale相当）は範囲外。ピンポイント編集＋部分再生成のハイブリッドで対応

### 5. 再生機能
- 楽譜上を**再生カーソル（青い線）が右に流れながら音が出る**、Finaleのような同期プレイバック
- 生成データに「音符ID ⇔ 再生タイミング」の対応マップを持たせて同期を実現

### 6. 印刷機能
- 生成された楽譜（フルスコア・パート譜）を印刷可能
- まずはブラウザSVG印刷（`@media print`）で対応、将来的にサーバーサイドPDF変換も検討

### 7. アクセス制御
- **未ログイン**: お試し利用可（回数制限、ジャンル数制限、パート譜出力不可、ダウンロード不可などを想定）
- **アカウント作成後**: 全機能解放
- 未認証ユーザーのデータはCognito Identity Pool（未認証ロール）で識別し、アカウント作成時に生成履歴を引き継げる設計

### 8. モバイル対応・操作性
- 「操作感」に強くこだわりたい最重要ポイントの1つ
- 楽譜そのものの直接編集はスマホでは無理をせず、専用UI（ピアノロール、スライダー、カードUI）に寄せる
- 楽譜表示は横スクロール＋ピンチズーム、横向き推奨などタッチ最適化
- ジェスチャー設計（スワイプで小節送り、ピンチでズーム、長押しで音符選択、ダブルタップで再生/一時停止）
- PWA化（ホーム画面追加、オフラインキャッシュ）も検討

### 9. デザイン
- こだわりを強く重視
- 楽譜部分は「伝統的・精密」、操作UI部分は「モダン・ミニマル」というコントラストのある方向性
- マイクロインタラクション（再生カーソルの動き、ジャンル切替のトランジション等）の質にこだわる
- DriftCraft本体とのトーン統一（緑系テーマ、ダークモード対応）を検討

---

## 技術スタック（推奨構成）

### フロントエンド
| 項目 | 技術 |
|---|---|
| フレームワーク | Next.js + TypeScript |
| 楽譜描画 | OpenSheetMusicDisplay（OSMD、MusicXML表示・カーソル機能） + 部分的にVexFlow（カスタム編集UI） |
| 音声再生 | Tone.js（Web Audio API） |
| 状態管理 | Zustand または Redux Toolkit |
| スタイリング | Tailwind CSS |
| PWA対応 | next-pwa |
| ホスティング | AWS Amplify Hosting または S3 + CloudFront |

### バックエンド／インフラ（AWS）
| 項目 | 技術 |
|---|---|
| API | API Gateway（REST or HTTP API） |
| 生成処理 | Lambda（ルールベースのアレンジエンジン） |
| データ保存 | DynamoDB（プロジェクト・編成設定・生成履歴） |
| ファイル保存 | S3（MusicXML、MIDI、生成音声、アップロード素材） |
| 認証 | Cognito（未認証Identity Pool → アカウント作成時に本登録へ引き継ぎ） |
| インフラ管理 | AWS CDK（TypeScript） |
| 非同期処理（将来） | SQS + WebSocket API（重い生成処理向け） |

### データフロー（生成エンジン出力）
```
[生成エンジン出力]
  ├─ MusicXML（楽譜表示用）
  ├─ MIDI/JSON（Tone.js再生用）
  └─ 対応マップ（音符ID ⇔ 再生時刻）→ カーソル同期用
```

### アレンジ生成アプローチ
- MVPは**ルールベース＋確率的テンプレート**を軸にする（ML内製は範囲外）
- ジャンルごとに「コンピングパターン」「ベースライン生成規則」「リズムテンプレート」を定義
- コード進行推定は簡易的な機能和声解析（キー推定＋コードトーン照合）で対応
- 「忠実度⇔崩し度」は確率的な置換ルールで制御（経過音・装飾音の挿入確率、小節境界での休符化確率など）
- 将来的なML導入時はMagenta（MusicVAE/PerformanceRNN等）のONNX化＋Lambda推論を検討

---

## MVP・フェーズ計画

| フェーズ | 内容 |
|---|---|
| 1 | メロディー入力(ピアノロール)→1ジャンル伴奏生成→固定小編成→楽譜表示(OSMD)、モバイル基本操作 |
| 2 | 複数ジャンル対応、パート譜分割出力、音声プレビュー(Tone.js)、デザインシステム確立 |
| 3 | 再生カーソル同期、編成プリセット複数化、印刷対応、部分編集UI(ピンポイント編集) |
| 4 | カスタム編成対応、崩し/ブレイクの高度制御、ジェスチャー操作の作り込み、PWA化 |
| 5 | 移調楽器・音域バリデーション精度向上、PDF高品質出力 |

**方針**: いきなりカスタム編成・フル機能から始めず、「固定編成・1ジャンルで一気通貫の体験を最短で作り切る」→段階的に自由度を上げる。

---

## コスト概算（個人開発規模での目安）

### 開発コスト（時間軸、週末中心の個人開発ペース）
- フェーズ1: 2〜3ヶ月
- フェーズ2: 1〜2ヶ月
- フェーズ3: 1.5〜2.5ヶ月
- フェーズ4: 2ヶ月〜
- フェーズ5: 1ヶ月〜
- ※フェーズ1〜3だけでも半年前後を想定。楽譜表示×再生同期×編成の柔軟性の組み合わせはエッジケースが多く、想定以上に時間がかかりやすい領域。

### 運用コスト（月額目安、小規模運用時）
- Lambda: 無料枠内に収まる可能性大
- API Gateway: 小規模なら数百円/月以下
- DynamoDB: オンデマンド課金で数百円/月以下
- S3: 数百円/月程度
- CloudFront: 無料枠(1TB/月)内に収まりやすい
- Cognito: MAU 50,000まで無料
- ドメイン: 年1,000〜2,000円程度
- **合計目安**: 月数百円〜1,000円台

---

## 開発体制の方針

- **ローカル開発**: VSCode + Claude Code をメインの開発環境とする
- **設計・要件詰めのフェーズ**: 対話しながら細かく調整が必要なため、ローカルのClaude Codeで進める
- **タスクが明確に切り出せる段階**: Claude Code on the web（クラウドサンドボックス、GitHub連携）も併用を検討
- Remote Control（ローカルセッションをスマホから操作する機能）とは別物なので混同しないこと

---

## 未決定・today's TODO候補

- ドメイン取得（`drift-score.com` 等）
- デザインシステムの詳細（トーン&マナー、参考サイトの選定）
- CDKプロジェクトの初期セットアップ
- DynamoDBのテーブル設計詳細
- Lambda関数の分割方針（アレンジ生成、楽譜変換、編成管理などの機能単位での分割）
- MusicXML生成部分の技術検証（どのライブラリ/自前実装で組むか）
